#!/usr/bin/env lunch

# starts miville and the selenium server. 
# prepare things up for a web distributed test
# vim: :set syntax=python 
# change to your own path to selenium server jar : 


PATH_TO_JAR = "/home/aalex/soft/selenium-remote-control-1.0.1/selenium-server-1.0.1/selenium-server.jar"
#PATH_TO_JAR = "/opt/selenium-remote-control/selenium-server.jar"

add_command("~/src/miville/branches/services/py/mivilled -o 0", title="miville zero", priority=1, sleep=1)
add_command("java -jar " + PATH_TO_JAR, title="selenium server")

