include $(top_srcdir)/globals.mak

#bin_PROGRAMS = allTester
#allTester_SOURCES = test.cpp  

#LDADD = $(ALL_GST_LIBS) $(JACK_LIBS) $(GTK_LIBS) ../libgst.a $(src)/gutil/libgutil.a $(src)/tcp/libtcp.a $(LIBUTIL) $(LIBAVC1394_LIBS) $(GL_LIBS) $(GLU_LIBS)

