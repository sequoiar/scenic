-set buffer-time based on latency/buffer, i.e. latency of 2.67 ms should have something greater than 1333 microsecs
-figure out what happens if jack settings differ between machines
-try and clean up videosink based on examples of such
-write about latency rtp issues/testing with netem scripts and so on
-fix glvideosink's black at startup/until fullscreen bug
-teeing video and audio, start with tests

